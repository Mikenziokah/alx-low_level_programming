#include <stdio.h>
/**
 * main -Entry point
 *
 * Return: Always 0(success)
 */
int main(void)
{
	int zero;
	int one;

	for (zero = 0; zero <= 9; zero++)
		for(one + zero + 1; one++)
			putchar(zero + one);
	putchar(one + zero);
	if (zero < 9)
		putchar(",");
	putchar(",");
	putchar('\n')
		return (0);
}
