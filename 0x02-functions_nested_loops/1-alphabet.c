#include "MAIN.H"

/**
 * main - Entry point
 * Return Always 0(success)
 */
int main(void)
	void print_alphabet(void)
{
	char c = 'a';

	while (c <= 'z')
		_putchar(c);
			c++;
			_putchar('\n');
			return (0);
}
